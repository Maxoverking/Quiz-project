export const STATUS = {
    idle: "idle",
    loading: "loading",
    success: "success",
    error: "error"
} as const;

export const QUIZ_STATUS = {
    start: "start",
    progress: "progress",
    end: "end",
} as const;
