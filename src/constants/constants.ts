export const STATUS = {
    idle: "idle",
    loading: "loading",
    success: "success",
    error: "Server Error!!!"
} as const;

export const QUIZ_STATUS = {
    choose: "/",
    start: "start",
    progress: "progress",
} as const;
